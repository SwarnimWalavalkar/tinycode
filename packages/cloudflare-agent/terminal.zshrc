# Interactive terminal defaults; agent commands continue to use the SDK shell.
PROMPT='%F{cyan}%~%f %# '
setopt interactivecomments
bindkey -e
alias ls='ls --color=auto'
source /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh
