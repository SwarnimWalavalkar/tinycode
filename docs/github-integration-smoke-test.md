# GitHub Integration Smoke Test

This test PR verifies that the durable agent sandbox can clone, commit, push, and open a pull request on GitHub using the configured account.

- Clone repository
- Create a new branch
- Add documentation file
- Commit with conventional commit message
- Push branch
- Open PR via `gh`

The purpose is to ensure end‑to‑end GitHub write capabilities are functional.
